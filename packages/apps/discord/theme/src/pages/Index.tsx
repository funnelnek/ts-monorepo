import React, { FC, Ref } from "react";


export const DiscordIndexPage: FC<any> = (props: any, ref: Ref<HTMLDivElement>): JSX.Element => {
    return (
        <div ref={ref}>

        </div>
    );
}