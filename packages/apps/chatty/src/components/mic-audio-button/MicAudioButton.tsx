import { Button, Icon } from "@funnelnek/ui";
import React from "react"


export const MicAudioButton = (props: any): JSX.Element => {
    return (
        <div>
            <Button />
        </div>
    );
}