import { TextField, Button } from "@funnelnek/ui";
import React from "react"

export const MessageInput = (props: any) => {
    return (
        <div>
            <TextField />
            <Button />
        </div>
    );
}