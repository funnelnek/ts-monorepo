import { Reflector } from "../../../src/contracts";
import { useInjector } from "../../../src/hooks";
import injector from "../../../src/Injector";


describe("useInjector", () => {
    beforeAll(() => {

    });

    it.todo("should return current injector context");
});